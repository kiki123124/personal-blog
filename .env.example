# API 管理员 Token（用于保护写操作 API）
# 在生产环境中请使用强随机字符串
ADMIN_TOKEN=your-secure-random-token-here

# 示例：生成随机 token 的方法
# Node.js: require('crypto').randomBytes(32).toString('hex')
# 或使用在线工具：https://generate-random.org/api-token-generator
